const Message = require('./Message');

// Define any associations (if needed)
// For example, if you want to associate a message with a user as sender and receiver
// You might use the User model and establish the relationships in your Users DB

module.exports = { Message };
